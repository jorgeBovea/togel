<%= form_for(instance_time) do |f| %>
  <% if instance_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instance_time.errors.count, "error") %> prohibited this instance_time from being saved:</h2>

      <ul>
      <% instance_time.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
